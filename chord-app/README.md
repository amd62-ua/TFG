# TFG
Desarrollo TFG

para iniciar  py -m venv venv   
 venv\Scripts\Activate     
pip install -r requirements.txt         
py -m pip install --upgrade pip
py -m streamlit run app/streamlit_app.py
deactivate
eliminar : Remove-Item -Recurse -Force venv